<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.rest.controlefacil">

    <application
        android:name=".ControleFacilApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.main.MainActivity" />
        <activity android:name=".ui.activity.init.InitActivity" />
        <activity android:name=".ui.activity.fotgot_password.ForgotPasswordActivity" />
        <activity android:name=".ui.activity.privacy_policies.PrivacyPoliciesActivity" />
        <activity android:name=".ui.activity.user.UserActivity" />
        <activity
            android:name=".ui.activity.category.CategoryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>
    </application>

</manifest>